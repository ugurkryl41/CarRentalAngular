import { RentalDatePipePipe } from './rental-date-pipe.pipe';

describe('RentalDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RentalDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
